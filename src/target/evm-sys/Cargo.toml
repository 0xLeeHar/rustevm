[package]
name = "evm-sys"
version.workspace = true
edition.workspace = true
authors.workspace = true

[features]
default = ["prague"]
prague    = []

[dependencies]
evm-sys-macros = { path = "../evm-sys-macros" }